import shopify_auth from './shopify_auth';
import callback from './callback';
import root from './root';
import install from './install';
import products from './products';

export default {
  root,
  install,
  products,
  callback,
  shopify_auth}
